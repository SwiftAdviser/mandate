{
  "name": "@mandate/eliza-plugin",
  "version": "0.1.0",
  "description": "ElizaOS plugin for Mandate policy-enforced on-chain actions",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@mandate/sdk": "workspace:*"
  },
  "peerDependencies": {
    "@elizaos/core": ">=0.1.0"
  },
  "peerDependenciesMeta": {
    "@elizaos/core": { "optional": true }
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
